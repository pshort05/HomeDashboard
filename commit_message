Add /health and /metrics endpoints

- GET /health returns {"status":"OK"}
- GET /metrics returns Prometheus text format (process, Python, GC metrics)
- homedashboard_requests_total counter tracks method/endpoint/status via after_request hook
- prometheus_client==0.21.1 added to requirements.txt
